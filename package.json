{
  "name": "fastapi-mcp-sse-worker",
  "version": "0.1.0",
  "description": "MCP SSE server for Cloudflare Workers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc && wrangler build"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20241127.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "wrangler": "^4.28.0"
  }
}
